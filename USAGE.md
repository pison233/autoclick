# 使用教程 — 原用法 + 新用法 实战详解

> 本文用**讲故事的方式**带你把两种用法从头到尾跑一遍：
> - **原用法**：用"步骤编辑器"搭一套通用自动化脚本（多步 + 条件 + 循环）
> - **新用法**：用「快捷操作」页的可视化流程构建器搭"看 → 判断 → 点"的流程（含 if/else 分支）
>
> 配套阅读：控件级说明见 [`MANUAL.md`](MANUAL.md)，设计思路见 [`DESIGN.md`](DESIGN.md)。

---

# 第一部分：先弄清四个基础概念

| 概念 | 大白话 |
|---|---|
| **窗口A / B** | 你绑定的两个软件窗口。工具只对绑定过的窗口操作。 |
| **识别区域** | 一块屏幕矩形，工具只"读"这块里的文字（OCR）。 |
| **点击位置** | 屏幕上一个点，工具会把鼠标移过去点一下。 |
| **步骤队列** | 右侧一列步骤，从上往下依次执行。它是"脚本本体"。 |

**贯穿全程的坐标规则**：识别区域和点击位置都记成"相对窗口左上角"的坐标。所以绑定的窗口**可以移动、最小化再还原**，步骤依然有效；但**别中途把窗口拖到另一块屏幕**导致坐标离屏。

---

# 第二部分：原用法（步骤编辑器）— 从零搭一套脚本

## 2.1 原用法能干什么

它是一条**指令流水线**：识别文字 → 存进变量 → 逻辑判断 → 点击/按键 → 循环。你可以任意组合，做成例如：
- 循环识别某个数值，低于阈值就点一下购买按钮；
- 识别界面状态 → 满足条件跳转，不满足走另一分支；
- 定时到点自动跑一套操作。

## 2.2 完整搭建流程（以"循环抢购"为例）

> 这个例子：**循环 10 次 → 识别价格 → 存进变量 price → 若 price<100 就置顶窗口并点击购买 → 结束**。

**第 1 步：管理员启动 + 验证 OCR**
1. 右键 exe → 以管理员身份运行。
2. 打开你要操作的软件/网页。
3. 验证识别能用（见第一部分前面）。

**第 2 步：绑定窗口 A**
1. 点左上**圆形图标（绑定A）** → 在目标窗口没被遮住的地方点一下。
2. 确认"窗口A："后出现目标窗口标题。

**第 3 步：选识别区域和点击位置（都在「A操作」页）**
1. 点「选择A识别区域」→ 在目标上框出会显示价格的那块。
2. 点「选择A点击位置」→ 点一下购买按钮位置。
3. 先用「识别A并保存」+「点击A选择位置」**各测一次**，确认框准了、点对了。

**第 4 步：把下面这些步骤按顺序加进队列**（在哪加我标在括号里）
1. 「添加循环开始」（**循环**页；次数先在左上"内循环次数"填 10）→ 队列加 `循环开始 次数 10`
2. 「添加A识别步骤」（A操作页）→ `A …识别区域…`
3. 在**保存**页"保存变量"填 `price` → 「添加保存A结果」→ `保存 A 结果 到键 "price"`
4. 在**条件**页表达式框填 `price < 100` → 「添加 If」→ `If price < 100`
5. 「添加置顶A步骤」（A操作页）→ `A 置顶窗口`
6. 「添加A点击步骤」（A操作页）→ `A … 点 (…)`
7. 回**条件**页 → 「添加结束if」→ `EndIf`
8. 回**循环**页 → 「添加循环结束」→ `循环结束`

加完，右侧队列从上到下应是：
```
循环开始 次数 10
A 识别区域…
保存 A 结果 到键 "price"
If price < 100
    A 置顶窗口
    A 点击…
EndIf
循环结束
```
（If 内的两行会自动缩进。）顺序不对就**按住左键拖动**调整；多了就右键删。

**第 5 步：运行**
1. 切到**步骤编辑**页。
2. "步骤队列执行次数"填整体重复次数，比如 `3`（= 整段再跑 3 遍）。
3. 点「执行全部步骤」。
4. 看右下「输出日志」逐条推进：识别结果、`price` 值、If 真/假、点击坐标。右下角「表达式运算日志」能看到 `price < 100` 的求值过程。

**第 6 步：停止（三种）**
- 界面「停止全部步骤(F12)」
- 键盘 **F12**
- **5 秒内右键 10 次**

**第 7 步：微调与保存**
- 右键某个步骤 →「编辑」→ 改延迟/坐标/表达式/跳出条件（只显示该步骤类型的字段）→ 保存。
- 配好满意的 → 点「保存队列」→ 填个备注 → 存成 JSON。
- 以后启动 → 右上角下拉框直接选中该文件快速加载，或点「加载队列」。
- 想定时自动跑：步骤编辑页设"启动时间"+「启用启动」→ 到点自动执行。

## 2.3 原用法各步骤"一句话语义"

| 步骤 | 出现于 | 执行时干什么 |
|---|---|---|
| `识别` | A/B操作 | 截窗口→OCR 读那块区域文字→更新该窗口"识别结果" |
| `点击` | A/B操作 | 把该窗口置前台→鼠标移到记录点（±随机）→按下抬起 |
| `置顶` | A/B操作 | 把该窗口还原(若最小化)+切到前台 |
| `保存…到键` | 保存页 | 把最近一次该窗口识别结果存进变量 |
| `If/ElseIf/Else/EndIf` | 条件页 | 表达式为真走本块，为假跳到下一分支 |
| `跳出If/Else` | 条件页 | 提前结束当前分支块 |
| `循环开始/结束` | 循环页 | 把中间步骤重复 N 次，可嵌套 |
| `跳出循环`/`跳入下次循环` | 循环页 | 提前退出 / 跳过本轮进下一轮 |
| `跳转到…`/`标签` | 跳转页 | 无条件跳到指定标签处 |
| `注释` | 注释页 | 灰色备注，执行时跳过 |
| `按键` | 按键页 | 按一下某个键 |

## 2.4 原用法最容易踩的坑

1. **点击没落在目标上** → 多半是目标窗口被别的窗口盖住，或最小化了。对策：在点击步骤前加「置顶」步骤。
2. **一直"未识别到数据"** → 识别区域太小、或画面闪太快没截到。对策：把区域框大一点、或加长识别步骤前的"延迟(ms)"。
3. **表达式报错"不支持大小比较"** → 字符串不能 `>`。对策：该识别步骤勾"只识别数字"，让结果是数字再比较。
4. **跑飞停不下来** → 勾选"识别为空则停止"，或靠 F12 / 右键×10 急停。
5. **整段要重复** → 用步骤编辑页的"步骤队列执行次数"，别在队列里套死循环。

---

# 第三部分：新用法（快捷操作）— 可视化流程构建器

## 3.1 它和原用法的区别

原用法（步骤编辑器）强在"什么都能排"，但要跨页签拼、上手门槛高。**快捷操作把最常用的"看 → 判断 → 点"做成一页可视化构建器**：

- 一排**节点**从上到下执行 = 一轮，跑完自动从头循环（不用手动加循环）。
- 节点种类少：**点击**、**检测(if)**、**否则**、**结束**；if/else 用缩进表示，和原版步骤队列观感一致。
- 点击全是**人类化时序**（HumanClicker，见 DESIGN.md），不用像原版那样逐个填延迟/偏移。
- 每个窗口的操作照旧走 A/B 绑定，检测窗口与点击窗口可不同（弹窗在辅助网页、按钮在游戏里也支持）。
- 窗口默认 1280 宽、可自由拉大。

## 3.2 搭建与运行流程（以"到价买入"为例）

**第 1 步：管理员启动 + 绑定窗口**
- 若检测（弹窗/价格）和操作（购买按钮）是两个窗口，就 **A、B 各绑一个**。

**第 2 步：在「快捷操作」页搭流程（点上方按钮一行行加）**

用到的四个按钮：**＋点击**（在某窗口某点连点 N 下）、**＋检测(if)**（框区域 + 条件，开一个分支）、**＋否则**（条件不满足时走这里）、**＋结束**（收掉分支）。

示例（到价就买、买完继续等）：
```
1  [检测] 窗口A 价格区域  条件: 当 数字≥阈值
2  [点击] 窗口B 购买按钮 ×3~5
3  [结束]
```
- 点第 1 行在右侧编辑：窗口 A/B → **重录区域**框出价格出现的那块地方 → **测试识别**按钮先试跑一次 OCR，看识别结果对不对 → **子条件**写"当 数字 ≥ 目标价"（可再加条件，用 且/或 连）→ **触发方式**选「每轮判断」还是「仅响应变化」。
- 想要"条件不满足做别的事"就加一行 **＋否则**：
```
1  [检测] 价格区域   数字<阈值 或 文字含"无货"   →满足
2  [点击] 购买按钮 ×3~5                          →真分支
3  [否则]                                        →不满足时走这里
4  [点击] 刷新按钮 ×1
5  [结束]
```

- **触发方式（每个检测单独选，单选）**
  - **每轮判断（默认）**：每轮都按当前状态走 真/否则，标准 if/else。适合要"不满足就走否则"的写法。
  - **仅响应变化**：只在"条件从无到有"瞬间走一次真分支、"从有到无"瞬间走一次否则。适合"等价格弹窗出现→买一次，弹窗停留期间不重复买"。
- 每个点击节点有"每轮连点 min~max"：同一点要快速点一组就填 3~5，普通点一次填 1。

**第 3 步：设停止条件（勾选多选，任一达到即停）**

| 条件 | 含义 | 建议 |
|---|---|---|
| 点击达到 N | 累计真实点击数 | 试跑先小 |
| 命中达到 N | 检测条件满足并走真分支计 1（Buy N 次就填 N） | 跑通后按你要的量 |
| 轮数达到 N | 整条流程完整跑 N 轮 | 看次数方便 |
| 运行达到 M 分钟 | 无人值守保险丝 | 挂机务必填 |
| 任意检测勾"满足即停" | 例如检测到"仓库已满"文字 → 立即停整流程 | 防跑飞 |

**第 4 步：保存 / 加载**
- 「保存流程」存成 JSON 到 `SavedQueues\QuickFlows\`；换机开发后「加载流程」直接回来（节点与停止条件一起恢复）。

**第 5 步：点「开始执行」，观察**
- 右侧状态实时显示"运行中：点击 N｜命中 M｜轮 K"。
- 左下日志逐条打印"第 X 行检测满足/不满足(条件，OCR=…)"、命中、停止原因。
- 停止：页面「停止」/ **F12** / **5 秒内右键 10 次**。
- 想看着它怎么走：把"轮数达到 1"勾上，跑一轮看日志即懂。

## 3.3 人类化时序在观察什么

快捷操作的每次点击都带 HumanClicker 时序（见 DESIGN.md）：

| 行为 | 表现 | 为什么 |
|---|---|---|
| 反应时间 | 检测命中后首个点击约 0.2~0.5s，逐次有波动 | 模拟人"看到→动手" |
| 连点 | 一个点击节点内按 min~max 次，间隔~0.2s 且有细微差 | 模拟人手快速点且不匀速 |
| 落点 | 大多分毫不差，偶发 ±1~2px | 手再稳也会偶尔小抖 |
| 走神 | 扫描中偶尔停顿 3~8s | 人不会永远目不转睛 |
| 核查 | 每 2~3 个有点击的轮停 3~6s | 人买几批会看一眼记录 |
| 会话漂移 | 长时间跑整体快慢有起伏 | 人状态会变 |

## 3.4 新用法调试清单（按顺序）

1. 先搭一小段（一个检测 + 一个点击），勾"轮数达到 1"跑一轮人工盯：
   - 用**测试识别**确认框的区域能读出你预期的文字/数字。
   - 条件不满足时日志出现"不满足…走否则"（或按你的触发方式跳过）。
   - F12 / 右键×10 / 停止按钮能立即停。
2. 看日志判断没触发是哪一种：
   - 检测区域 OCR 恒空 → 区域框错或框到无字区，重框或用测试识别验证。
   - 条件一直不满足 → 数字/文字阈值不对（例如 OCR 出的是"60"你却写了"数字<66"）；或触发方式选了"仅响应变化"而弹窗从来没出现过。
   - 点到了但没买成 → 点击位置偏，重录。
3. 都正常再放大命中/轮数参数挂机。

## 3.5 快捷操作页"还没做"的部分（将来）

- 查看交易记录目前只有停顿占位；真正的"移到记录按钮→点击→回购买界面"（含鼠标轨迹模拟）在后续扩展，见 DESIGN.md。
- 每条子条件目前都在同一个检测区域上判断；若需要"不同区域各自判断后组合"，可拆成多个 检测(if) 嵌套。

# 第四部分：什么时候用哪个

| 场景 | 用哪个 |
|---|---|
| 盯价格/状态 → 满足点/不满足点（if/else） | **快捷操作页**（可视化流程构建器，已内置人类化时序） |
| 极复杂：手动循环 / 跳转 / 表达式 | **原用法步骤编辑器** |
| 要定时无人值守 | 步骤编辑器页的定时任务（两种都能跑完后套用） |
| 想验证 OCR 准不准 | 「识别A并保存」（调试） |

**提示**：快捷操作与步骤编辑器共用同一个"绑定窗口 / OCR / 防卡死 / F12"体系，但**互不干扰**——你可以先用步骤编辑器测 OCR，再去快捷操作页正式跑。
