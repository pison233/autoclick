# 使用教程 — 原用法 + 新用法 实战详解

> 本文用**讲故事的方式**带你把两种用法从头到尾跑一遍：
> - **原用法**：用"步骤编辑器"搭一套通用自动化脚本（多步 + 条件 + 循环）
> - **新用法**：用「快捷操作」页做"盯价格弹窗 → 到价就买"的专用流程
>
> 配套阅读：控件级说明见 [`MANUAL.md`](MANUAL.md)，设计思路见 [`DESIGN.md`](DESIGN.md)。

---

# 第一部分：先弄清四个基础概念

| 概念 | 大白话 |
|---|---|
| **窗口A / B** | 你绑定的两个软件窗口。工具只对绑定过的窗口操作。 |
| **识别区域** | 一块屏幕矩形，工具只"读"这块里的文字（OCR）。 |
| **点击位置** | 屏幕上一个点，工具会把鼠标移过去点一下。 |
| **步骤队列** | 右侧一列步骤，从上往下依次执行。它是"脚本本体"。 |

**贯穿全程的坐标规则**：识别区域和点击位置都记成"相对窗口左上角"的坐标。所以绑定的窗口**可以移动、最小化再还原**，步骤依然有效；但**别中途把窗口拖到另一块屏幕**导致坐标离屏。

---

# 第二部分：原用法（步骤编辑器）— 从零搭一套脚本

## 2.1 原用法能干什么

它是一条**指令流水线**：识别文字 → 存进变量 → 逻辑判断 → 点击/按键 → 循环。你可以任意组合，做成例如：
- 循环识别某个数值，低于阈值就点一下购买按钮；
- 识别界面状态 → 满足条件跳转，不满足走另一分支；
- 定时到点自动跑一套操作。

## 2.2 完整搭建流程（以"循环抢购"为例）

> 这个例子：**循环 10 次 → 识别价格 → 存进变量 price → 若 price<100 就置顶窗口并点击购买 → 结束**。

**第 1 步：管理员启动 + 验证 OCR**
1. 右键 exe → 以管理员身份运行。
2. 打开你要操作的软件/网页。
3. 验证识别能用（见第一部分前面）。

**第 2 步：绑定窗口 A**
1. 点左上**圆形图标（绑定A）** → 在目标窗口没被遮住的地方点一下。
2. 确认"窗口A："后出现目标窗口标题。

**第 3 步：选识别区域和点击位置（都在「A操作」页）**
1. 点「选择A识别区域」→ 在目标上框出会显示价格的那块。
2. 点「选择A点击位置」→ 点一下购买按钮位置。
3. 先用「识别A并保存」+「点击A选择位置」**各测一次**，确认框准了、点对了。

**第 4 步：把下面这些步骤按顺序加进队列**（在哪加我标在括号里）
1. 「添加循环开始」（**循环**页；次数先在左上"内循环次数"填 10）→ 队列加 `循环开始 次数 10`
2. 「添加A识别步骤」（A操作页）→ `A …识别区域…`
3. 在**保存**页"保存变量"填 `price` → 「添加保存A结果」→ `保存 A 结果 到键 "price"`
4. 在**条件**页表达式框填 `price < 100` → 「添加 If」→ `If price < 100`
5. 「添加置顶A步骤」（A操作页）→ `A 置顶窗口`
6. 「添加A点击步骤」（A操作页）→ `A … 点 (…)`
7. 回**条件**页 → 「添加结束if」→ `EndIf`
8. 回**循环**页 → 「添加循环结束」→ `循环结束`

加完，右侧队列从上到下应是：
```
循环开始 次数 10
A 识别区域…
保存 A 结果 到键 "price"
If price < 100
    A 置顶窗口
    A 点击…
EndIf
循环结束
```
（If 内的两行会自动缩进。）顺序不对就**按住左键拖动**调整；多了就右键删。

**第 5 步：运行**
1. 切到**步骤编辑**页。
2. "步骤队列执行次数"填整体重复次数，比如 `3`（= 整段再跑 3 遍）。
3. 点「执行全部步骤」。
4. 看右下「输出日志」逐条推进：识别结果、`price` 值、If 真/假、点击坐标。右下角「表达式运算日志」能看到 `price < 100` 的求值过程。

**第 6 步：停止（三种）**
- 界面「停止全部步骤(F12)」
- 键盘 **F12**
- **5 秒内右键 10 次**

**第 7 步：微调与保存**
- 右键某个步骤 →「编辑」→ 改延迟/坐标/表达式/跳出条件（只显示该步骤类型的字段）→ 保存。
- 配好满意的 → 点「保存队列」→ 填个备注 → 存成 JSON。
- 以后启动 → 右上角下拉框直接选中该文件快速加载，或点「加载队列」。
- 想定时自动跑：步骤编辑页设"启动时间"+「启用启动」→ 到点自动执行。

## 2.3 原用法各步骤"一句话语义"

| 步骤 | 出现于 | 执行时干什么 |
|---|---|---|
| `识别` | A/B操作 | 截窗口→OCR 读那块区域文字→更新该窗口"识别结果" |
| `点击` | A/B操作 | 把该窗口置前台→鼠标移到记录点（±随机）→按下抬起 |
| `置顶` | A/B操作 | 把该窗口还原(若最小化)+切到前台 |
| `保存…到键` | 保存页 | 把最近一次该窗口识别结果存进变量 |
| `If/ElseIf/Else/EndIf` | 条件页 | 表达式为真走本块，为假跳到下一分支 |
| `跳出If/Else` | 条件页 | 提前结束当前分支块 |
| `循环开始/结束` | 循环页 | 把中间步骤重复 N 次，可嵌套 |
| `跳出循环`/`跳入下次循环` | 循环页 | 提前退出 / 跳过本轮进下一轮 |
| `跳转到…`/`标签` | 跳转页 | 无条件跳到指定标签处 |
| `注释` | 注释页 | 灰色备注，执行时跳过 |
| `按键` | 按键页 | 按一下某个键 |

## 2.4 原用法最容易踩的坑

1. **点击没落在目标上** → 多半是目标窗口被别的窗口盖住，或最小化了。对策：在点击步骤前加「置顶」步骤。
2. **一直"未识别到数据"** → 识别区域太小、或画面闪太快没截到。对策：把区域框大一点、或加长识别步骤前的"延迟(ms)"。
3. **表达式报错"不支持大小比较"** → 字符串不能 `>`。对策：该识别步骤勾"只识别数字"，让结果是数字再比较。
4. **跑飞停不下来** → 勾选"识别为空则停止"，或靠 F12 / 右键×10 急停。
5. **整段要重复** → 用步骤编辑页的"步骤队列执行次数"，别在队列里套死循环。

---

# 第三部分：新用法（快捷操作）— 专用"盯价抢购"

## 3.1 它和原用法的区别

原用法是"你手动编排每步"；**快捷操作是内置好的一套循环**，你给它四个信息：
1. **检测窗口** + **检测区域**——在哪个窗口上等信号（弹窗/数字出现在哪块固定区域）；
2. **操作窗口** + **操作位置**——信号到了往哪个窗口的目标上操作。

其中"检测窗口"和"操作窗口"**可以不是同一个窗口**（例如弹窗显示在辅助网页上、要点的按钮在游戏里）。剩下的循环它自己跑：扫描 → 信号出现 → 像人一样反应 → 连点一组 → 信号消失再武装 → 累计够就停。

## 3.2 搭建与运行流程

**第 1 步：管理员启动 + 绑定窗口**
- 启动、绑定窗口（见 2.2）。若检测与操作是两个窗口，就 **A、B 各绑一个**（比如检测=网页 A、操作=游戏 B）。

**第 2 步：切到「快捷操作」页，逐项配置**

① **检测窗口** 下拉选 A（或 B）→ 点**「选择检测区域」**框出信号出现的那块固定位置。
- 关键：这块区域平时应**是空的**，只有到价/出信号才出现数字。别框到一直有数字的静态区，否则会误判"一直在出信号"。
- 识别区域越小越准越快。

② **操作窗口** 下拉选 B（或 A，可与检测同窗口）→ 点**「选择操作按钮」**在目标（如购买按钮）上点一下。
- 之后操作落点 = 这个点 ± 偶发 1~2px 抖动，务必确认能点中目标。

③ **填基础参数**

| 参数 | 默认 | 含义 | 建议 |
|---|---|---|---|
| 一组操作次数 min~max | 3~5 | 每次信号出现点几下 | 按你手动一组的量填，先 3~5 |
| 目标次数 | 0 | 累计点满自动停 | 先填 `30` 试跑；跑通再填 `300~400` 或留 0 |
| 最长运行(分钟) | 0 | 无人值守保险 | 挂机务必填，如 `120` |

④ **（可选）高级设置**：点开「高级设置」，可微调反应延迟、连点间隔、按压停留、抖动概率/幅度、走神、核查间隔与停顿、会话漂移幅度。不点开则用默认（已按真人调优）。

**第 3 步：点「开始执行」**

开始后它会：
```
一直扫「检测窗口」的检测区域（OCR 间隔由推理耗时自然决定）
↓
区域出现数字（信号）且处于"武装"状态
↓ 像人一样顿约 0.2~0.5s
在「操作窗口」目标上连点 3~5 下（落点偶发抖动）→ 累计+3~5
↓
信号消失 → 重新武装（同一个信号只触发一次）
↓ 每 2~3 组停顿 3~6s（模拟查看记录，目前只停顿）
重复，直到：累计到目标 / 超最长运行 / 手动急停
```

**第 4 步：观察并停止**
- 右侧状态实时显示"`运行中：累计 N`"。
- 左下输出日志逐条打印"`快捷操作：检测到(…)…操作 N 次`"。
- 停止：页面「停止」按钮 / **F12** / **5 秒内右键 10 次**。

## 3.3 人类化时序在观察什么

这是快捷操作区别于原版点击的地方（HumanClicker 实现，见 DESIGN.md）：

| 行为 | 表现 | 为什么 |
|---|---|---|
| 反应时间 | 弹窗出现→首击约 0.2~0.5s，逐次有小波动 | 模拟人"看到→动手" |
| 爆发连点 | 一组内 3~5 下，间隔~0.2s 且有细微差 | 模拟人快速点鼠标，且不匀速 |
| 落点 | 大多分毫不差，偶发 ±1~2px | 手再稳也会偶尔小抖 |
| 走神 | 扫描中偶尔停顿 3~8s | 人不会永远目不转睛 |
| 核查 | 每 2~3 组停 3~6s | 人买几批会看一眼记录 |
| 会话漂移 | 长时间跑整体快慢有起伏 | 人状态会变 |

## 3.4 新用法调试清单（按顺序）

1. 先填目标 `30`、最长运行 `1`，跑一轮人工盯：
   - ✅ 弹窗出现后约半秒内开点、落点在按钮上
   - ✅ 弹窗消失不再连点（等待下一个）
   - ✅ F12 / 右键×10 能立即停
2. 看日志判断**没触发**是哪种：
   - 完全没"检测到弹窗"→ 弹窗区域框错或框到静态数字区，重框。
   - 有"检测到弹窗"但没买到 → 购买按钮位置偏，重选。
3. 都正常再放大参数挂机。

## 3.5 快捷操作页"还没做"的部分（将来）

- 查看交易记录目前**只有停顿占位**，真正的"移到记录按钮→点击→回购买界面"（含鼠标轨迹模拟）列入后续扩展，见 DESIGN.md。

---

# 第四部分：什么时候用哪个

| 场景 | 用哪个 |
|---|---|
| 只是"盯一个价格弹窗 → 到价买" | **快捷操作页**（简单、已内置人类化时序） |
| 要多步 + 分支 + 自定义流程 | **原用法步骤编辑器** |
| 要定时无人值守 | 步骤编辑器页的定时任务（两种都能跑完后套用） |
| 想验证 OCR 准不准 | 「识别A并保存」（调试） |

**提示**：快捷操作与步骤编辑器共用同一个"绑定窗口 / OCR / 防卡死 / F12"体系，但**互不干扰**——你可以先用步骤编辑器测 OCR，再去快捷操作页正式跑。
