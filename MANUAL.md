# MoliGod 自动化助手 — 完整操作手册

> 本文档穷举软件中**所有可操作的元素**，每个都说明：操作方式 / 会怎么显示 / 作用 / 原理 / 必要性 / 是否原功能。
>
> **图例**：✅ = 原功能（作者 moligod 原始设计，我们未改动）｜🆕 = 我们新增（人类化自动购买 + HumanClicker）
>
> 阅读前建议先看 [`DESIGN.md`](DESIGN.md) 了解整体设计思路。界面为中文，宽 1080、固定尺寸，左右分栏：左侧上部=窗口绑定区，左侧中部=功能 Tab（多页签），右侧=步骤队列，底部=日志。

---

## 〇、主窗口总览

| 区域 | 内容 | 说明 |
|---|---|---|
| 左上（Row0） | 窗口绑定区 | 绑定A/B 两个圆形图标 + 已绑窗口标题 |
| 左上（Row1） | 使用事项 | 加载队列时显示其备注（默认隐藏） |
| 左中（Row2） | **TabControl** | 11 个功能页签（见下文逐页） |
| 右上（Col1 Row0-2） | 步骤队列 | 下拉快速加载 + 步骤 ListBox（可拖拽/右键） |
| 左下（Row3 Col0） | 输出日志 | 运行/识别日志（RichTextBox）+ 复制按钮 |
| 右下（Row3 Col1） | 表达式运算日志 | 条件/表达式求值过程展示 |

---

## 一、窗口绑定区（主窗口顶部，非 Tab）

### 1.1 圆形图标「绑定A」（左上第一个同心圆）
- **操作方式**：鼠标左键**点击**圆形图标 → 屏幕被全屏透明取点窗覆盖 → 把鼠标移到目标窗口上 → **单击左键**。
- **会怎么显示**：取点窗是透明无边框全屏（十字光标）。成功后左上"窗口A："右侧显示所绑窗口的标题文本。
- **作用**：把"窗口A"绑定到你点中的那个窗口；之后所有带 A 的操作/步骤都作用于它。
- **原理**：`OverlayPickWindow` 全屏捕获鼠标点击的屏幕坐标 → `WindowFromPoint` 找到该坐标下的窗口句柄 → `GetAncestor(GA_ROOT)` 提升为**顶层窗口**句柄 → 存入 `_boundAHwnd`，`UpdateBoundTitles()` 读取并显示标题（`GetWindowText`）。
- **必要性**：**必需**——这是所有 A 类操作的前提，不绑定会提示"请先绑定窗口A"。
- **是否原功能**：✅ 原功能

### 1.2 圆形图标「绑定B」（左上第二个）
- 同 1.1，唯一区别是绑定到 `_boundBHwnd`（"窗口B"），供 B 类操作使用。✅ 原功能

> **原理补充（坐标体系）**：绑定后，识别区域、点击位置都以**窗口左上角为原点**存为"窗口相对坐标"；真正执行时再用 `GetWindowRect` 把窗口当前位置加上去换算成屏幕坐标。所以窗口移动/拖到别处，步骤依然有效。

---

## 二、Tab「A操作」（与「B操作」结构完全一样，B 是同一套镜像）

### 2.1 识别结果行
- 显示区「A识别结果：_OcrResultTextA」。识别到→黑字显示文本；空→红字"未识别到数据"。**信息展示，非按钮**。✅ 原功能

### 2.2 「选择A识别区域」
- **操作方式**：点按钮 → 弹出全屏**框选窗**（十字光标）→ 按住左键在目标窗口上拖出一个矩形 → 松开。
- **会怎么显示**：日志输出"`A已选择区域：WxH`"。
- **作用**：设定 A 的 OCR 识别区域（只识别这块区域里的文字）。
- **原理**：`OverlaySelectWindow` 返回屏幕矩形 → 与 `GetRect(窗口)` 求交集 → 转成窗口相对坐标存入 `_rectA`。区域在窗口外则拒绝。
- **必要性**：OCR 类操作的**前提**。识别区域越小 OCR 越快。
- **是否原功能**：✅ 原功能

### 2.3 「添加A识别步骤」
- **操作方式**：点按钮（需先绑定+选区域）。
- **显示**：右侧步骤队列新增一行"`A 延 …→识别…区域 (x,y,w,h)`"，可能有"(仅数字)/(ROI复用)"标注。
- **作用**：把"识别这块区域"作为一个步骤加入执行队列。
- **原理**：把 `_rectA` + 当前参数（延迟/随机/数字模式/ROI复用）封装成 `ScriptStep(Type=Ocr, Target=A)`，加入 `_steps`，刷新列表。
- **必要性**：搭建 A 自动化流程的**常用**手段。
- **是否原功能**：✅ 原功能

### 2.4 「识别A并保存」
- **操作方式**：点按钮（需绑定）。
- **显示**：A识别结果更新；日志显示识别出的文本与"已保存：`jietu/jietu_时间戳.png`"。
- **作用**：单次测试识别：截全窗口 → 识别 _rectA 区域 → 把结果画红框的整图存成 png 供人工核对。
- **原理**：`CaptureWindow` 截取窗口位图 → `BitmapConverter.ToMat` → 裁剪 ROI → `_ocr.OcrAsync` 推理 → 用 `Graphics` 在原图画红框 → 存盘。**不加入队列**，只做一次测试。
- **必要性**：调参/验证识别准确率时的**调试工具**。
- **是否原功能**：✅ 原功能

### 2.5 「只识别数字」复选框（_OcrAOnlyNums）
- **操作方式**：勾选/取消。
- **作用**：该 A 的 OCR 只输出数字与逗号，且取**最长**的一段数字串返回（适合读价格/数量等数值）。
- **原理**：识别出的所有文本行里筛出数字/逗号字符，空串丢弃，按长度降序取第一个。默认勾选。
- **必要性**：**可选**——纯文本场景取消即可。
- **是否原功能**：✅ 原功能

### 2.6 「ROI未变化复用OCR结果」复选框（_ReuseOcrRoiCacheCheck）
- **操作方式**：勾选/取消。
- **显示**：执行日志里识别项出现"`(复用)`"或"`(重新识别)`"。
- **作用**：性能优化——识别区域画面没变化时，跳过 OCR 直接复用上次文本。
- **原理**：`ComputeRoiSignature` 把 ROI 灰度化→缩放 48×48→SHA256，比对缓存签名（以 step 为 key）。B 操作的此项与 A **双向绑定共用**。
- **必要性**：**可选优化**。注意有极小概率漏判画面微变，官方建议只在画面变化明显的场景用。
- **是否原功能**：✅ 原功能

### 2.7 「选择A点击位置」
- **操作方式**：点按钮 → 全屏取点窗 → 在目标窗口内点一下。
- **显示**：日志"`A已选择点击位置：x,y`"。
- **作用**：设定 A 的鼠标点击目标点。
- **原理**：`OverlayPickWindow` 返回屏幕点 → 校验在窗口内 → 转窗口相对坐标存 `_clickA`。
- **必要性**：点击类操作**前提**。
- **是否原功能**：✅ 原功能

### 2.8 「添加A点击步骤」
- 把当前点击点+参数封装成 `ScriptStep(Type=Click)` 加入队列，列表显示"`A …→ 点 (x±rx,y±ry) 停 dwellms`"。✅ 原功能

### 2.9 「点击A选择位置」（试点）
- **操作方式**：点按钮。
- **作用**：立即在刚才选定的点击位置**真实点一次**，验证坐标对不对。
- **原理**：窗口相对坐标 + 窗口当前位置 → 屏幕坐标 → `IsIconic` 判断最小化则还原(SW_RESTORE) → `SetForegroundWindow` 置前 → `ClickAtScreen`（SetCursorPos + mouse_event 按下/抬起）。
- **必要性**：**调试工具**。
- **是否原功能**：✅ 原功能

### 2.10 「添加置顶A步骤」
- 加入一个"把窗口A调到前台"的步骤（`BringFront`）。执行时还原最小化 + SetForegroundWindow。适合需要先切到目标窗口再点的情况。✅ 原功能

### 2.11 A 参数输入行（新建 A 步骤时默认带入）
| 控件 | 含义 | 默认 |
|---|---|---|
| 鼠标随机偏移 X / Y（_RandomXA/YA） | 点击落点 ±随机像素 | 0 |
| 延迟(ms)（_DelayMsA）± 随机（_RandomDelayA） | 执行该步骤前固定等 N ms，可再 ±随机 | 300 / ±0 |
| 停留(ms)（_DwellMsA）± 随机（_RandomDwellA） | 鼠标按下后按住停留时长 | 100 / ±0 |

- **原理**：`GetRandomVal(基准, 随机范围)` 用**均匀分布**在基准值上下取随机。这些是**原版的随机方案**（与🆕 HumanClicker 的对数正态方案不同；原步骤编辑器仍用这套，未改动）。
- **必要性**：模拟人手抖动/节奏不均，**可选**。
- **是否原功能**：✅ 原功能

> **B操作 Tab 说明**：从 2.2~2.11 全部镜像一套给"窗口B"，控件带 B 后缀（`_rectB/_clickB/_boundBHwnd` 等），行为完全一致，不重复列出。✅ 原功能

---

## 三、Tab「条件」

### 3.1 表达式输入框（_ExprBox）
- **操作方式**：点击输入。焦点进入时下方浮出灰色帮助文字（_ExprHelpText），失焦收起。
- **显示**：提示支持 `== != > >= < <=  AND(&&) OR(||)`；示例 `A1 == '文本' OR B1 > 100`。
- **作用**：输入 If/ElseIf 的布尔表达式。
- **原理**：见 3.2 求值原理。变量名（如 A1、B1）是此前"保存"步骤写入的变量，文本要用引号。
- **必要性**：条件逻辑的**输入入口**。
- **是否原功能**：✅ 原功能

### 3.2 「添加 If」/「添加 ElseIf」/「添加 Else」/「添加结束if」
- **操作方式**：在表达式框填好条件后点"添加 If"或"添加 ElseIf"；无条件的"Else"、"结束if(EndIf)"直接点。
- **显示**：右侧队列按缩进显示 `If <表达式>`、`ElseIf`、`Else`、`EndIf`（If/Loop 内的步骤缩进）。
- **作用**：搭建 if/else 分支结构。
- **原理**：运行前预编译跳转表（`nextBranch`/`endIfByBranch`/`endIfContaining`）；If/ElseIf 用 `EvaluateExpression` 求值：为真走当前块，为假跳到下一个分支；嵌套用栈配对。
- **必要性**：分支逻辑的**必需**部件。
- **是否原功能**：✅ 原功能

### 3.3 「跳出If/Else」
- 加一个 `BreakBlock` 步骤：运行时跳到最近的 EndIf 之后（`endIfContaining[idx]`）。用于提前结束分支块。✅ 原功能

### 3.4 表达式求值原理（EvaluateExpression）
- 用 `System.Data.DataTable.Compute()` 求值。预处理：中文引号→单引号，`||→OR`、`&&→AND`、`==→=`、`!=→<>`。变量能解析为数字则替换成数字，否则替换成带引号字符串。显式禁止字符串大小比较（会提示开启"只识别数字"）。右下"表达式运算日志"会逐段显示求值过程。✅ 原功能

---

## 四、Tab「循环」

### 4.1 内循环次数（_LoopInnerCount）
- 输入"添加循环开始"时默认使用的循环次数，默认 3。✅ 原功能

### 4.2 「添加循环开始」/「添加循环结束」
- 加 `LoopStart`(带次数)/`LoopEnd` 步骤，两者配对成一段可重复的步骤块。✅ 原功能

### 4.3 「跳出循环」
- 加 `BreakLoop`：运行时跳到最近一层循环的结束之后。✅ 原功能

### 4.4 「跳入下次循环」
- 加 `ContinueLoop`：跳过本次剩余步骤直接进入该层循环下一轮。✅ 原功能
- **循环原理**：运行前配对 `LoopStart→LoopEnd` 成字典；运行时用栈记 `(起点, 剩余次数)`，`LoopEnd` 递减计数并跳回起点，次数到则出栈。支持嵌套。✅ 原功能

---

## 五、Tab「跳转」

### 5.1 标签名称（_LabelBox）+「添加标签」/「添加跳转」
- 输入名字 → "添加标签"生成 `Label <名>:` 步骤；"添加跳转"生成 `跳转到 <名>`。
- **原理**：运行前把所有 Label 位置建成 `labelIndex` 字典；遇到 Goto 按名字 O(1) 跳到对应下标。找不到标签则报错并停止。✅ 原功能

---

## 六、Tab「注释」

### 6.1 注释内容（_CommentText）+「添加注释步骤」
- 输入任意文字 → 加入一个灰色显示的 `注释: <文字>` 步骤。执行时**跳过**（无操作），仅作队列里的人工备注。✅ 原功能

---

## 七、Tab「保存」

### 7.1 保存变量（_SaveKey）+「添加保存A结果」/「添加保存B结果」
- 输入变量名（如 `myVal`、`A1`）→ 点按钮把一个"保存步骤"加入队列。
- **作用**：把最近一次 A（或 B）的 OCR 结果存入该变量，供条件表达式引用。
- **原理**：执行到该步骤时把 `lastOcrA`/`lastOcrB` 写入 `_vars[键]`；条件框里写 `A1 > 100` 即读取此值。
- **必要性**：把"识别结果"接上"逻辑判断"的**桥梁**。
- **是否原功能**：✅ 原功能

---

## 八、Tab「按键」

### 8.1 按键名称（_KeyPressBox）+「添加按键步骤」
- 输入按键名（Enter/F1/A 等，按 WPF `Key` 枚举名）→ 加入按键步骤。
- **执行原理**：`Enum.TryParse` 解析成虚拟键 → `keybd_event` 按下，停 dwell，再抬起。
- ✅ 原功能

### 8.2 「识别按键」
- **操作方式**：点按钮（按钮文字变"请按键..."）→ 按一下要录的键。
- **作用**：把按下的键自动填入按键名称框，省去手敲键名。
- **原理**：`MainWindow_PreviewKeyDown` 在 `_identifyingKey` 期间捕获首个按键写回 `_KeyPressBox`。✅ 原功能

---

## 九、Tab「自动购买」（🆕 我们新增）

> 这整页是我们实现的人类化自动购买，对应 `HumanClicker.cs` + `StartAutoBuy_Click`。设计依据见 DESIGN.md。

### 9.1 目标窗口（_AutoBuyTargetCombo）
- **操作方式**：下拉选 A 或 B（**必须**与你绑定的窗口一致）。
- **作用**：指定对哪个绑定窗口执行自动购买。
- **原理**：`SelectedIndex==0` 取 `_boundAHwnd` 否则 `_boundBHwnd`。🆕 新增

### 9.2 「选择弹窗区域」
- **操作方式**：点按钮 → 全屏框选 → 框出**价格弹窗出现的那块固定位置**。
- **显示**：旁边文字从"未选择"变为"`宽x高`"。
- **作用**：设定自动购买的**检测区**——这块区域一旦出现数字（弹窗），就认为"到价了"。
- **原理**：`OverlaySelectWindow` 选屏上矩形 → 转窗口相对坐标存 `_autoBuyRect`。
- **必要性**：**必需**，不选无法开始。
- **是否原功能**：🆕 新增

### 9.3 「选择购买按钮」
- **操作方式**：点按钮 → 全屏取点 → 在购买按钮上点一下。
- **显示**：旁边文字变为"`x,y`"。
- **作用**：设定连点购买的落点（中心）。
- **原理**：`OverlayPickWindow` → 存 `_autoBuyPoint`（窗口相对坐标）。🆕 新增

### 9.4 「一组点击次数」min ~ max（_AutoBuyBurstMin/_AutoBuyBurstMax）
- **操作方式**：两个数字框，默认 3 ~ 5。
- **作用**：每次弹窗出现爆发连点**几**下（在 min~max 间随机取一个数）。
- **原理**：调 `HumanClicker.SetBurstRange(min,max)`，实际次数 `BurstCount()`=均匀取整（默认 3~5）。🆕 新增

### 9.5 「目标点击次数」（_AutoBuyTargetClicks）
- 累计点击达到该值自动停。填 0 = 不限，一直跑到手动停。**单位=点击次数**。🆕 新增

### 9.6 「最长运行(分钟)」（_AutoBuyMaxMinutes）
- 运行超过该分钟数自动停（无人值守保险丝）。填 0 = 不限。🆕 新增

### 9.7 「开始自动购买」（_StartAutoBuyButton）
- **操作方式**：上述都配好后点它；运行中按钮变"运行中..."，再点无效。
- **显示**：右侧状态文字实时变"`运行中：累计点击 N`"；下方输出日志逐条打印"检测到弹窗(价格)…点击 N 次"。
- **作用**：启动自动购买主循环。
- **原理**（状态机，运行在后台线程）：
  1. 循环 OCR 弹窗区域（数字模式）；
  2. `armed && 弹窗出现` → 置 `armed=false` → `Sleep(ReactionDelayMs)`(人类化反应 ~200ms/下限120/AR1 自相关) → `ClickBurst` 爆发连点 → 累计计数 → 每 2~3 组 `MaybeCheckRecords` 停 3~6s（核查占位，TODO）；
  3. 弹窗消失 → `armed=true`（重新武装，保证**一次弹窗只触发一次**）；
  4. 到目标次数 / 到最长分钟 / `_stopAll` → 退出。
- **必要性**：本页**核心按钮**。
- **是否原功能**：🆕 新增

### 9.8 「停止」
- **操作方式**：点它（复用步骤编辑页同一个停止逻辑）。
- **作用**：置 `_stopAll=true`，主循环下一轮退出。
- **原理**：即全局急停体系之一（见 §12）。🆕 页面提供（机制沿用原功能）

> **人类化时序要点**（对比原版均匀随机）：🆕 HumanClicker 用**对数正态分布**采样反应/间隔/停留，带 **AR(1) 自相关**与**会话级 tempo 漂移**；点击落点只在 ~12% 概率下 ±1~2px 抖动（高频连点几乎不动）。与 A/B 操作里原版的"±均匀随机"是两套思路，互不影响。

---

## 十、Tab「步骤编辑」

### 10.1 步骤队列执行次数（_LoopCount）
- 整个步骤列表重复执行 N 次，默认 1。✅ 原功能

### 10.2 「识别为空则」复选框（_BreakOnEmpty）
- 勾选后：任一 OCR 步骤结果为空 → 立即停止整个脚本（防死循环）。✅ 原功能

### 10.3 「防卡死(5秒内右键10次)」复选框（_FailSafeCheck）
- **操作方式**：勾选/取消，默认勾选。
- **作用**：勾选时，运行中你在**屏幕任意位置 5 秒内右键 10 次**可强制停止脚本。
- **原理**：运行开始 `InstallHook` 装系统级低级鼠标钩子 `WH_MOUSE_LL`；钩子回调被动计数右键，滑动窗口≥10 置 `_stopAll`。**它不产生点击、不保活，纯监听**。结束即卸载（`UninstallHook`）。取消勾选则整段运行不装任何钩子。
- **必要性**：无人值守/跑飞脚本的**保险**（鼠标通道急停）。可取消。
- **是否原功能**：✅ 原功能

### 10.4 「执行全部步骤」
- 按顺序跑完步骤列表（LoopCount 次）。`Task.Run` 后台执行 + `Dispatcher` 更新 UI；运行前注册 F12 热键、装防卡死钩子。✅ 原功能

### 10.5 「清空全部步骤」
- 清空步骤队列与 ROI 缓存。✅ 原功能

### 10.6 「停止全部步骤(F12)」（_StopAllButton）
- 置 `_stopAll=true`；按钮文字会随绑定的热键变化（如 Ctrl+F5）。✅ 原功能

### 10.7 「绑定停止快捷键」
- **操作方式**：点按钮 → 按下想用的组合键（可单独 Ctrl/Shift，或 Ctrl/Alt/Shift+某键）。
- **作用**：自定义全局停止热键，替代默认 F12。
- **原理**：`PreviewKeyDown`/`PreviewKeyUp` 捕获按键组合 → `RegisterHotKey` 注册系统级热键（仅运行时注册）。单 Ctrl/Shift 特殊处理（无法用 RegisterHotKey，仅窗口聚焦时生效）。✅ 原功能

### 10.8 「保存队列」/「加载队列」
- 保存：弹**输入说明对话框**(InputDialog，可写队列使用备注) → 存 JSON 到 `SavedQueues` 目录。网络步骤会被过滤不保存。
- 加载：读 JSON（兼容旧格式）；加载后顶部"使用事项"区显示备注。
- **原理**：`StepDto` 序列化/反序列化（坐标、参数、表达式全部保存）。✅ 原功能

### 10.9 「启动GPU加速」复选框（_UseGpuCheck）
- 勾选：请求 OCR 用 DirectML 显卡加速（`onnxruntime-directml`）；不勾强制 CPU。切换会重启 OCR 子进程。
- **原理**：设置 `OnnxOcrHelper.UseGpu` → 环境变量 `ONNX_USE_GPU=1` → Python 端优先 `DmlExecutionProvider`，失败自动回退 CPU。默认关（先 CPU 验证，再开 GPU 提速）。✅ 原功能

### 10.10 定时任务区
- **启动时间**（时/分/秒 三个数字框）+「启用启动」：到点自动"执行全部步骤"。早于当前时间→默认次日。之后每日顺延。
- **停止时间** +「启用停止」：到点自动停止脚本。
- **原理**：`DispatcherTimer` 每秒检查目标时刻；只有启用时计时器才运行。状态显示在右侧 `_ScheduleStatusText`。
- **必要性**：无人值守定时运行的**可选**增强。
- ✅ 原功能

---

## 十一、Tab「关于」

- 版本/作者/反馈群信息展示；三个**超链接**（赞助、GitCode、GitHub）点击用系统浏览器打开（`Process.Start` + UseShellExecute）。✅ 原功能（纯信息页）

---

## 十二、右侧「步骤队列」面板

### 12.1 下拉快速加载（_QuickLoadCombo）+「刷新」+「目录」
- 下拉列出 `SavedQueues` 目录下的队列 JSON，选中即加载。
- 「刷新」重扫目录；「目录」用资源管理器打开该文件夹。✅ 原功能

### 12.2 步骤列表（_StepsList）
- **显示**：每条一行的格式化文本；If/Loop 内缩进；注释灰色。
- **操作方式**：
  - **单选**：单击某行。
  - **多选**：按住 **Ctrl** 点选多行。
  - **拖拽排序**：选中后按住左键拖动到目标位置释放（可拖多条）。
  - **右键菜单**：编辑 / 复制步骤 / 删除 / 执行所选步骤。
- **原理**：拖拽用 WPF DragDrop 交换 `_steps` 顺序；Ctrl 多选是 `SelectionMode=Extended`。
- **必要性**：步骤编排的主工作区。
- ✅ 原功能

### 12.3 右键「编辑」
- 弹出 **编辑步骤** 对话框（见 §14），按步骤类型只显示相关字段，可改坐标/参数/表达式/跳转条件等；点"重选位置/重选识别位置"可重新取点。✅ 原功能

### 12.4 右键「复制步骤」
- 深拷贝选中步骤插到其后（复制一份同样的步骤）。✅ 原功能

### 12.5 右键「删除」
- 删除选中步骤（含 ROI 缓存清理）。✅ 原功能

### 12.6 右键「执行所选步骤」
- 只执行被选中的那几步（不跑整队）。✅ 原功能

---

## 十三、底部日志区

### 13.1 输出日志（_OutputBox）+「复制全部日志」
- RichTextBox 只读滚动显示所有运行/识别日志，带时间戳；"未识别到数据"相关字段标红。
- 日志同时**写入文件** `jietu/app.log`；异常写入 `jietu/error.log`。
- 「复制全部日志」把面板文本复制到剪贴板。
- **原理**：后台线程调用 `AppendLog` 入队，`Dispatcher.BeginInvoke` 批量刷到 UI（避免卡界面）；日志过长自动清段。
- ✅ 原功能

### 13.2 表达式运算日志（_ExprLogBox）
- 只读文本框，显示每个 If/表达式求值时的变量映射与分段计算结果。✅ 原功能

---

## 十四、全屏取点/框选悬浮窗（非按钮，是被调起的工具）

### 14.1 取点窗 OverlayPickWindow（点圆形图标/选点击位置时弹出）
- **操作方式**：全屏透明、无边框、十字光标；鼠标移到目标处单击左键即返回该点屏幕坐标。
- **原理**：`AllowsTransparency` 的置顶全屏窗，`MouseLeftButtonUp` 里 `PointToScreen` 取坐标并关窗返回。✅ 原功能

### 14.2 框选窗 OverlaySelectWindow（选识别/弹窗区域时弹出）
- **操作方式**：按住左键拖出红色矩形框，松开返回屏幕矩形。
- **原理**：`Canvas` + 红 `Rectangle`，鼠标按下/移动/抬起画框，`PointToScreen` 换算物理坐标。✅ 原功能

---

## 十五、编辑步骤对话框（EditStepWindow，右键「编辑」打开）

> 面板按步骤类型显示（代码里 `switch(_step.Type)` 控制各 Panel 显隐）。

| 字段/控件 | 作用于哪些步骤 | 说明 |
|---|---|---|
| 延迟(ms) ± 随机（TxtDelay/TxtRandomDelay） | 除 LoopEnd/Save 外几乎都显示 | 步骤执行前等待 |
| 停留(ms) ± 随机（TxtDwell/TxtRandomDwell） | Click、KeyPress | 按下按住时长（KeyPress 时标签变"按压(ms)"） |
| 点击位置 X/Y +「重选位置」（TxtPointX/Y、BtnPickPoint） | Click | 点击目标点 |
| 随机偏X/Y（TxtRandomX/Y） | Click | 落点 ±随机 |
| 识别区域 X/Y/W/H +「重选识别位置」（TxtRectX/Y/W/H、BtnSelectRect） | Ocr | 识别矩形 |
| 只识别数字（ChkOcrNums） | Ocr | 数字模式 |
| ROI未变化复用（ChkReuseOcrRoi） | Ocr | ROI 缓存 |
| 循环次数（TxtCount） | LoopStart | 循环次数 |
| 正则模式/表达式（TxtPattern，标签随类型变） | Condition/Expression/IfStart/ElseIf/Comment | 内容按类型 |
| 变量键名（TxtKey） | Condition/Save | 取变量来源/保存目标 |
| 按键 +「识别按键」（TxtKey、BtnIdentifyKey） | KeyPress | 按键名（识别捕获） |
| 条件满足则跳出（ChkJump） | Condition/Expression | JumpOnTrue |
| 网卡名称/操作/同步执行（网络三件套） | Network（半成品） | 见下 |

> **⚠️ Network 步骤是半成品**：界面（编辑对话框）可配置网卡/断开恢复，但执行循环里 `Network` 动作**只 Sleep 无实际断网调用**；保存/加载时也会被过滤。属于原作者未完成功能。✅ 原功能（占位）

- 「保存」写回步骤并关窗；「取消」放弃。✅ 原功能

---

## 十六、保存队列的说明输入对话框（InputDialog）

- 点「保存队列」后弹出：文本框输入该队列的"使用事项/备注"，可选填；「确定/取消」。
- 备注随 JSON 保存，加载后显示在窗口左上"步骤队列使用事项"。✅ 原功能

---

## 十七、全局热键与急停汇总

| 触发 | 何时生效 | 作用 |
|---|---|---|
| F12（或自定义热键） | 仅脚本运行时（注册后全局） | 停止脚本 |
| 5 秒内右键 10 次 | 仅运行且勾选"防卡死"时 | 停止脚本（鼠标通道） |
| 「停止」/「停止全部步骤」按钮 | 界面可用时 | 停止脚本 |

两者为**键盘/鼠标双通道冗余急停**，互不替代。✅ 原功能机制

---

## 版本对照速查

| 功能 | 是否原功能 | 备注 |
|---|---|---|
| 窗口绑定 A/B | ✅ | 一切前提 |
| A/B 操作（识别/点击/置顶/参数） | ✅ | 原版均匀随机 |
| 条件/循环/跳转/注释/保存/按键 | ✅ | 步骤编排 |
| 步骤队列（拖拽/右键/执行） | ✅ | 主工作区 |
| 定时任务 / GPU 开关 / 日志 | ✅ | 增强与监控 |
| 防卡死 / F12 急停 | ✅ | 双通道急停 |
| **自动购买页 + HumanClicker** | 🆕 我们新增 | 人类化时序，见 DESIGN.md |
